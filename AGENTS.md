# AGENTS.md

## 1. 项目概述
一段话说清楚：项目是什么、技术栈、仓库结构。
前 10 行必须让 AI 建立项目心智模型。
📝 编码规范 (Code Style)：风格统一的基础
🏗️ 项目架构 (Project Architecture)：结构设计的参考

## 2. 快速命令
构建、启动、格式化、质量检查的命令速查表。
环境变量配置说明（env 文件位置、启动脚本自动 source）。

## 3. 后端架构
包结构树（ASCII）+ 每个包的用途注释。
核心子系统的简要说明 + 详细文档链接。
前后端术语映射（如有差异）。

## 4. 前端架构
技术栈、路由方案、API 层约定、组件库规范。
详细文档链接。

## 5. 关键约定
5-10 条硬性编码规则（违反会直接导致问题的）。
每条规则附详细文档链接。

## 6. 本地开发及验证流程
「改 → 构建 → 启动 → 验证」的完整闭环。
curl 验证模板、Token 获取、日志路径。

## 7. 质量检查
lint、format、build、test 命令矩阵。

## 8. 参考项目约定
参考项目列表 + 优先级规则。

## 9. 文档导航
所有详细文档的索引表。

---

## 🤖 AI 协作约定
1.对话语言：默认使用中文交流；若用户或项目文档指定其他语言，则跟随更具体的语言要求。

2.代码生成：生成代码必须遵守上述编码规范，并在 PR 前通过 Checkstyle/PMD 检查。

3.测试优先：新增功能必须同步生成单元测试；修改逻辑需更新相关测试。

4.文档同步：新增/修改接口需同步更新 Swagger/OpenAPI 注解。

5.性能感知：编写 SQL 和循环时，考虑时间复杂度和 N+1 查询问题。

6.安全第一：涉及用户输入时，必须考虑 XSS、SQL 注入、权限校验等安全防护。

---

### 💡 使用建议

1. **放置位置**：将上述内容保存为 `AGENTS.md`，放在项目根目录。
2. **自定义填充**：将 `[请填写项目名称]` 等占位符替换为你项目的实际信息。
3. **工具集成**：配置 Checkstyle/PMD 插件，并与 CI/CD 集成，确保规范落地。
4. **持续迭代**：当 AI 犯错或发现新的最佳实践时，及时更新本文件。

这份 `AGENTS.md` 既是一份 AI 指引，也是一份**团队开发规范**，能有效降低 AI 生成代码的“噪音”，提升协作效率。如果你需要针对特定场景（如微服务、大数据、前端等）定制，可以在此基础上扩展。
